<ParamField header="Authorization" type="string" required>
  Bearer token. Format: `Bearer ${AAS_API_KEY}`. Sir gets the key from the dashboard under **Settings → API Keys**, or by calling `GET /api/v1/admin/credentials` once authenticated. Each key authorises one tenant only — Sir's tenant.
</ParamField>
