
<Note>
  The base URL for all API requests is `https://app.alfred.black`. All endpoints are prefixed with `/api/v1/`.

  If you have direct Tailscale access to your instance, you can also use `https://<tailscale-hostname>:3100`.
</Note>
