
<Note>
  The base URL for all API requests is `https://<your-tailscale-hostname>:3100`. Replace
  `<your-tailscale-hostname>` with your instance's Tailscale hostname (e.g., `alfred-acme.tailnet.ts.net`).
</Note>
