
<Note>
  All API calls go to `https://alfred.black/api/v1/...`. The SaaS layer authenticates the request, decrypts Sir's tenant key, and proxies the call over Tailscale to Sir's dedicated `ctrl-api` on port `:3100`. Sir never touches the tenant directly. The same `Authorization: Bearer ${AAS_API_KEY}` header is required on every request.
</Note>
