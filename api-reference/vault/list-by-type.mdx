---
title: "List Records by Type"
description: "List all vault records of a specific type."
api: "GET /api/v1/vault/list/{type}"
---

## Overview

Returns a list of all records of a given type (e.g., `person`, `task`, `decision`). Use this to browse records by category.

## Path Parameters

<ParamField path="type" type="string" required>
  The record type to list. Must be one of:
  - Entity types: `person`, `org`, `project`, `task`, `event`, `note`, `location`, `process`, `account`, `asset`, `conversation`, `input`, `run`, `session`
  - Learning types: `assumption`, `decision`, `constraint`, `contradiction`, `synthesis`
</ParamField>

## Response

<ResponseField name="raw" type="string">
  List of records as text. If the output is structured JSON, it is returned directly instead.
</ResponseField>

## Request Example

<RequestExample>

```bash cURL
curl -X GET /api/v1/vault/list/person \
  -H "Authorization: Bearer alf_your_key_here"
```

```javascript JavaScript
const type = 'person';

const response = await fetch(`/api/v1/vault/list/${type}`, {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer alf_your_key_here'
  }
});

const data = await response.json();
console.log(data);
```

</RequestExample>

## Response Example

<ResponseExample>

```json 200 OK
{
  "raw": "person/Alice Chen.md\nperson/Bob Martinez.md\nperson/Carol Zhang.md"
}
```

```json 400 Bad Request
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Unknown vault type: unknown_type. Known types: person, org, project, task, event, note, location, process, account, asset, conversation, input, run, session, decision, assumption, constraint, contradiction, synthesis"
  }
}
```

```json 401 Unauthorized
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Unauthorized"
  }
}
```

</ResponseExample>
