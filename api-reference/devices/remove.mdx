---
title: "Remove Device"
description: "Remove a paired device from the OpenClaw gateway"
api: "DELETE /api/v1/devices/{deviceId}"
---

Removes a paired device from the OpenClaw gateway, revoking its access and invalidating any existing tokens.

<Note>
  This endpoint uses `deviceId` in the URL path (not `requestId`). This is the identifier of an already-paired device, not a pending request.
</Note>

<ParamField path="deviceId" type="string" required>
  The unique identifier of the paired device to remove.
</ParamField>

<RequestExample>

```bash cURL
curl -X DELETE https://app.alfred.black/api/v1/devices/dev_a1b2c3d4e5 \
  -H "Authorization: Bearer YOUR_API_KEY"
```

</RequestExample>

<ResponseExample>

```json Response
{
  "message": "Device removed"
}
```

</ResponseExample>

<ResponseField name="message" type="string">
  Human-readable status message confirming the device has been removed.
</ResponseField>
